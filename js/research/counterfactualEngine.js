export class CounterfactualEngine { analyzeClosedTrades(trades=[]){ if(!trades.length)return{summary:"No closed trades to analyze.",rows:[]}; const rows=trades.slice(-50).map(t=>{const mfe=+(t.maxProfit||0),actual=+(t.pnl||0),risk=Math.max(.01,+(t.riskDollars||1));const protectedExit=mfe>risk*.7?Math.max(actual,mfe*.5):actual;const trailExit=mfe>risk*1.2?Math.max(actual,mfe*.65):actual;const best=Math.max(actual,protectedExit,trailExit);return{id:t.id,symbol:t.symbol,actual,protectedExit,trailExit,missed:best-actual,lesson:best>actual?"Management could have improved outcome.":"Actual management acceptable."};});const missed=rows.reduce((a,r)=>a+r.missed,0);return{summary:`Counterfactual review found approximately $${missed.toFixed(2)} of missed management value across ${rows.length} trades.`,rows}; } }
